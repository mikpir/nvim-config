-- Options are automatically loaded before lazy.nvim startup
-- Default options that are always set: htt-- then you need to set the option below.
vim.g.lazyvim_picker = "snacks"
vim.grepprg = "rg --with-filename --no-heading"
